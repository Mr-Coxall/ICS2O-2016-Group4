MusicSoundScene = class()

function MusicSoundScene:init(x)
    -- you can accept and set parameters here
    self.x = x
end

function MusicSoundScene:draw()
    -- Codea does not automatically call this method
end

function MusicSoundScene:touched(touch)
    -- Codea does not automatically call this method
end
